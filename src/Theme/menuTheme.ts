import {createTheme} from "@material-ui/core";

export const menuTheme = createTheme({
    palette:{
        primary:{
            main: "#ffffff",
        },
        secondary:{
            main: "#A7EBF4",
        }
    },
})